<form method="post" action="{{ path('app_breathing_exercice_delete', {'id': breathing_exercice.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ breathing_exercice.id) }}">
    <button class="btn">Delete</button>
</form>
